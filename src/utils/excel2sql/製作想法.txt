@startuml
start
	:drop sequence "表格名稱"+"_SEQ"+"數量";
	:CREATE SEQUENCE +"表格名稱"+"_seq"+"計數器"
		INCREMENT BY "增量"
		START WITH "起始數"
		NOMAXVALUE
		NOCYCLE
		NOCACHE
	;
end
@enduml


@startuml
partition init {
(*)-->"讀取table_excel資料"
-->"把資料put在一個HashMap or List裡面"

}

@enduml
-->"把一個List放在HashMap裡面"


@startuml






(*) --> " "
partition SEQ { 
	--> "drop sequence '表格名稱'+'_SEQ'+'數量'"
	--> "
			CREATE SEQUENCE +'表格名稱'+'_seq'+'計數器'
		    INCREMENT BY '增量'
		    START WITH '起始數'
		    NOMAXVALUE
		    NOCYCLE
		    NOCACHE
		"
	--> "用一個MAP紀錄"
}

partition Orchestra #CCCCEE {
    --> "INSERT"
    --> "判斷類型"
    --> if "" then
    		-->[名子]"取得名子資料"
    	else 
    	    -->[PK]"取得SEQ"
    	else
    	    -->[FK]"取得PK對象的值" 
    	else
    	    -->[日期]"note right:有發布日期、結束日期的差別。"
    	else
    	    -->[NULL]"VAL=NULL"
    	endif
}

@enduml

# 要記得完成網頁板
- 上傳
- 規格書
- radio選單

# 智能判斷Excel

# 問題 現在不能判斷FK 對應的 PK是第幾個






































